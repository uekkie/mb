.card.mt-4
  .card-body.p-3
    = simple_form_for([post, comment]) do |f|
      = f.input :content, as: :text, input_html: { rows: 3 }, required: true
      = f.button :submit, class: 'btn btn-primary'
